---
title: "Fenced Hash"
updated: "2026-07-07"
---

## Write markdown that contains headings

```markdown
## not-a-step

This heading lives inside a code fence and must not split the walkthrough.
```

The fence above contains a literal h2 marker.

## Confirm the split held

```php
$steps = 2;
```

Only two steps should exist.
